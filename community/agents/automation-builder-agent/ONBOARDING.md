# Onboarding

Run `/setup` first. Setup initializes operating files, registry/schema assets, recurring crons, memory, goals, heartbeat/event visibility, and `${CTX_ROOT}/state/${CTX_AGENT_NAME}/.onboarded`.
