# Local State

Runtime-only local state can live here when a connector or workflow needs a small durable file.

Do not store API keys, session cookies, private tokens, or credentials in this directory.
