# Social Media Config

Setup writes runtime config here:

- `brand-profile.json`
- `platform-config.json`
- `content-calendar.json`

Use the schemas in `../schemas/` and examples in `../examples/` as the contract. Do not store secrets here.
