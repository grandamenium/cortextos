# Onboarding

Run `/setup` on first boot. Setup configures repositories, remotes, branch/worktree rules, tests, CI, deployment boundaries, local operating files, tasks, heartbeat, memory, events, and approvals.

Setup must touch `${CTX_ROOT}/state/${CTX_AGENT_NAME}/.onboarded` after it completes.
