# Fitness Agent

Read `AGENTS.md` first. It is the source of truth for startup, safety, task, memory, and fitness operating rules.

If the user says `/setup`, `setup`, or `run setup`, read `.claude/skills/setup/SKILL.md`, which delegates to `.claude/skills/fitness-setup/SKILL.md`.
