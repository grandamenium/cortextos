# System Context

Filled during `/setup`.

Default timezone: configured by `config.json` as `{{timezone}}`.

Local file source of truth:

- profile: `fitness/profile.json`
- plans: `fitness/plans/`
- check-ins: `fitness/checkins/`
- logs: `fitness/logs/`
- reviews: `fitness/reviews/`
