// AUTO-GENERATED by `cortextos ecosystem`. Do NOT edit by hand.
// Re-run `cortextos ecosystem` to regenerate.
//
// Note: env vars use process.env.X || 'default' so PM2 picks up the value
// from the calling shell at startup time. This means `CTX_INSTANCE_ID=foo
// pm2 restart cortextos-daemon` switches instances without regenerating.
module.exports = {
  apps: [
    {
      name: 'cortextos-daemon',
      script: "/Users/subbu_ai_assistant/cortextos/dist/daemon.js",
      args: '--instance ' + (process.env.CTX_INSTANCE_ID || "default"),
      cwd: "/Users/subbu_ai_assistant/cortextos",
      env: {
        // Merged 2026-05-17: kept hardcoded form from `cortextos ecosystem` output
        // (this file is auto-generated; regen on the target host to update paths)
        // and added MacBook-side env vars for crash-trigger debugging + multi-machine guard.
        CTX_INSTANCE_ID: process.env.CTX_INSTANCE_ID || "default",
        CTX_ROOT: process.env.CTX_ROOT || "/Users/subbu_ai_assistant/.cortextos/default",
        CTX_FRAMEWORK_ROOT: "/Users/subbu_ai_assistant/cortextos",
        CTX_PROJECT_ROOT: "/Users/subbu_ai_assistant/cortextos",
        CTX_ORG: process.env.CTX_ORG || "subbu-ops",
        // Debug-only: set to '1' to enable SIGUSR2 signal → controlled
        // uncaughtException for testing the crash-visibility path
        // (.daemon-crashed markers + crash-loop operator Telegram alert).
        // Leave '0' in production; enable temporarily to reproduce crash
        // paths during development. `kill -SIGUSR2 $(pm2 pid cortextos-daemon)`
        // then watch the operator chat for "🚨 CRITICAL: daemon crash-looping"
        // after 3 crashes in 15 min.
        CTX_DEBUG_ALLOW_CRASH_TRIGGER: '0',
        // Multi-machine: refuse to spawn agent dirs that aren't in
        // enabled-agents.json. Prevents framework-relay-mirrored Mac mini
        // agent dirs from auto-spawning on MacBook (bug 1778770012799).
        CTX_REQUIRE_EXPLICIT_ENABLE: '1',
      },
      max_restarts: 50,
      restart_delay: 5000,
      autorestart: true,
    },
    {
      name: 'cortextos-dashboard',
      script: 'npm',
      args: 'run dev',
      cwd: "/Users/subbu_ai_assistant/cortextos/dashboard",
      env: {
        PORT: process.env.PORT || '3000',
      },
      // Dashboard reads its real config from dashboard/.env.local — populated
      // by /onboarding Phase 7. PM2 just supervises the npm process.
      max_restarts: 50,
      restart_delay: 5000,
      autorestart: true,
    },
  ],
};
