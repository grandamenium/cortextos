# RESEARCH — What references the current framework path

**Last audited: 2026-04-18** (post-upstream-merge, 8-agent fleet)

## Method
`grep -rln "/Users/joshweiss/cortextos"` across the framework tree and `~/.cortextos/`, excluding node_modules, dist/, log files, and the socket.

## Active hardcoded paths (MUST rewrite)

### 1. `ecosystem.config.js` (5 lines) — CONFIRMED current as of 2026-04-18
```
line 11:  script: "/Users/joshweiss/cortextos/dist/daemon.js"
line 13:  cwd: "/Users/joshweiss/cortextos"
line 17:  env.CTX_FRAMEWORK_ROOT: "/Users/joshweiss/cortextos"
line 18:  env.CTX_PROJECT_ROOT: "/Users/joshweiss/cortextos"
line 29:  dashboard cwd: "/Users/joshweiss/cortextos/dashboard"
```
All must become `/Users/joshweiss/code/cortextos/...`. Note: ecosystem.config.js is auto-generated by `cortextos ecosystem` — do NOT re-run that command post-move, just sed the file directly.

### 2. `~/.cortextos/cortextos1/dashboard.env` and `~/.cortextos/default/dashboard.env` — CONFIRMED current
Both contain `CTX_FRAMEWORK_ROOT=/Users/joshweiss/cortextos`. Both must be rewritten. (cortextos1 is the active instance; default is legacy but rewrite both to be safe.)

### 3. PM2 process registry (in-memory + `~/.pm2/dump.pm2`)
PM2 caches cwd. A `pm2 delete ... && pm2 start ecosystem.config.js` is required — `pm2 restart` alone will not re-read the cwd.

### 4. Global npm symlink `/opt/homebrew/bin/cortextos`
After the move: `npm unlink -g cortextos` then `npm install -g .` in new location.

### 5. Agent configs under `orgs/clearworksai/agents/*/config.json`
`working_directory` fields point at `~/code/auditos` etc. These are REPOS, not framework paths. Unchanged. Now 8 agents: auditos, frank2, hunter, larry, maven, muse, sage, sre.

### 6. `~/.cortextos/` runtime state
No framework-path references inside config. Stays put.

## New since April 13

### 7. `state/` directory in framework tree
Added in upstream merge. Contains framework-level state. Moves with `~/cortextos/` — no special handling needed.

### 8. `installers/` directory
Added in upstream merge. Also moves cleanly.

### 9. New transcript path after move
Post-move, new agent sessions will write transcripts under `~/.claude/projects/-Users-joshweiss-code-cortextos-orgs-clearworksai-agents-{name}/`. Old transcripts stay at old path — they're historical and fine.

## Inactive references (ignore)

- Log files under `~/.cortextos/*/logs/*/stdout.log` — historical, will roll
- `inbound-messages.jsonl` — historical message bodies
- `dashboard/.next/` build artifact — regenerated on next build
- Processed inbox files — historical

## What depends on git remote / GitHub

- `origin` = `grandamenium/cortextos.git` (upstream, no-push)
- `upstream` = `git@github.com:grandamenium/cortextos.git` (SSH, for upstream pulls)
- `fork` = `https://github.com/clearworks-ai/cortextos.git` (what Josh pushes to via SSH workaround)
- `.git/` moves with the dir — no remote changes needed.
- Current branch: `merge/upstream-main-apr17`. Must merge to main before executing migration.

## cortextos.old status — CONFIRMED SAFE TO REMOVE

`diff <(ls ~/cortextos/) <(ls ~/cortextos.old/)` shows cortextos.old is missing `installers/` and `state/` (both added after the copy was made). No unique data. Safe to `rm -rf ~/cortextos.old/` during Phase 6.

## Running state at move time (updated for 8 agents)

- `cortextos-daemon` (pm2, instance cortextos1) — owns all 8 agent PTYs. Restart = all agents die cleanly and respawn.
- `cortextos-dashboard` (pm2) — Next.js, stateless aside from SQLite under `~/.cortextos/*/dashboard.db`.
- 8 agents: auditos, frank2, hunter, larry (claude-opus-4-7), maven, muse, sage, sre — all Claude Code subprocesses under daemon PTY.
- Active transcripts under `~/.claude/projects/` will diverge post-move (old paths become historical).

## Time estimate

- Pre-flight check + freeze: 5 min
- Move + path rewrites: 10 min
- PM2 restart + npm re-link: 3 min
- Smoke test all 8 agents + dashboard: 10 min
- Rollback on standby.

**Total planned outage: ~15 min.**
