# cortextOS code quality standards (P9 principal eng)

Adopted + adapted from `sb-claude-jarvis/.claude/rules/code-quality.md` 2026-05-08. Each rule is the *class of trap*, not a per-symptom fix. Trust the rules across language boundaries — most are TypeScript-as-much-as-Python. cortextOS-specific micro-retros appended after the language-agnostic block.

## Universal rules

- **File size — soft 300, hard 500.** Validator should enforce 500 hard. At 300, ask *should this split?* Cohesive single-purpose files (parsers, dispatch tables, state machines, schema dataclasses) can sit 300–500. Mixed-responsibility 300+ files must split.
- **Functions under 40 lines.** Hard rule. Extract helpers if longer.
- **Module package over single file when complexity grows.** `src/foo.ts` → `src/foo/` directory once it grows beyond basic CRUD. Index via `src/foo/index.ts`.
- **Single responsibility.** Storage doesn't render. Views don't mutate. Commands are thin wrappers around library functions. If a 300+ file mixes responsibilities, split — size is the symptom.
- **No incremental monoliths.** Before adding to a 300+ file, refactor first if you'd push past 500 OR add a new responsibility.
- **Edge cases and error paths.** Handle missing files, bad IDs, empty state. Never crash on user input. Test failure paths, not happy paths.
- **Wire features in the same commit as the primitive.** "Phase 1 = primitive only" is the failure mode — split into smaller goals where each commit ships value to a real caller.
- **Verify upstream framework conventions before porting.** Copying validation/field/layout from another framework (Express, NestJS, langchain) requires fetching the *target* framework's docs first.
- **Session-restart immunity.** State files keyed on `session_id` are load-bearing tech debt — sessions die and resume. Use `(abs_path, wallclock_bucket)` or content hashes as join keys. `session_id` may appear as a forensic tag, never as a lookup key.
- **One code path for critical operations.** If session-start / restart / notify-send is called from CLI + watchdog + worker, extract a shared function. Inline copies diverge silently. (Micro-retro from upstream jarvis 2026-04-16: watchdog reimplemented `cmd_start()` inline → 7-defect crash-restart loop.)
- **Subprocess spawning — explicit env, not bash -c.** Don't wrap commands in `bash -c` in worker contexts — bash inherits a minimal PATH (especially under launchd or systemd). Pass an explicit env dict with PATH prepended, or use the runtime's structured spawn API (Node `child_process.spawn` with `env: {...}`).
- **Verify git-tracking state before locking a path decision.** If a design says "repo-canonical" / "versioned" / "git-diffable", run `git check-ignore <path>` first. Same trap with submodules, worktrees, separately-init'd inner repos.
- **Never auto-create canonical objects from arbitrary text references.** Promotion to a canonical store (entity files, db rows, vault items) must be explicit (user action) or human-confirmed via a triage inbox. Auto-extraction goes to a queue, not directly to the store.
- **LLM workers spawned with vague triage/synthesis instructions hallucinate convincing-looking false data.** Outputs look right (proper format, urgent statuses, plausible names, specific dates) and are wrong. Constrain workers to cite source paths/line numbers for every claim, or don't spawn them. Hallucinations cascade — downstream workers treat fabricated facts as ground truth.
- **Filesystem reindexers must respect quarantine/exclusion conventions.** A `glob("**/*.md")` that scans every subdir will re-include files you just moved to `_quarantine/`, `_deprecated/`, `.trash/`. Maintain an `INDEX_EXCLUDE_DIRS` set and check it per-component.
- **Numeric validation from JSON/TOML has multiple traps in one line.** TypeScript: `typeof x === 'number'` is true for `NaN`, `Infinity`, `-Infinity`, and integers AND floats — distinguish via `Number.isFinite(x) && Number.isInteger(x)` for counts, `Number.isFinite(x) && x > 0` for positive durations. Naive validators that accept `true` (Python int subclass) or `NaN` (TS number) leak through. Pattern: explicit predicate per case (counts, durations, percentages).
- **Best-effort batch creators must be checked at the call boundary.** Helpers that "create N things, return what we got" silently leak partial state when callers assume success implies completeness. Pattern: either the helper raises/returns null on partial completion, OR the caller does `if (out.length !== EXPECTED) bail`. Don't return half a structure and pretend it's whole.
- **Functions that mutate external systems must be guarded by an upstream idempotency check before re-entry.** A retry, a partial-write recovery, or a manual re-invocation will create duplicates unless the caller checks "is the work already done?" first. Pattern: a sentinel field in local state gates the write. Push the gate to the call site where local truth lives, don't try to make the writer itself idempotent across all upstream APIs.
- **Translating object A → object B with explicit field-list silently drops fields the upstream adds later.** When you write `out = { id: t.id, title: t.title, ... }`, the test that checks each named field passes forever, but the moment upstream `t` gains a new field that callers downstream depend on, your translation drops it without a peep. **Pattern fix:** `out = { ...source, ...overrides_for_renamed_fields, derivedField: x }` — spread first, override only what differs, never re-list fields that pass through unchanged. **Test fix:** at every translation boundary, add at least one positive round-trip assertion: `expect(out.description).toBe(...)` not just `expect(out.id).toBe(t.id)`.
- **When two parallel state machines track the same entity, the cleanup-side must update the *other* one through its own API — not just kill the underlying resource.** Both sides being correct in isolation does not prevent disagreement when combined. Pattern: if you reuse another subsystem's spawn primitive (workers/, processes/, watchdog/), you also own updating its terminal state through its API on the way out — don't just yank the resource out from under it.
- **Integration tests must read the artifact the production consumer reads, not just the side-channel state the test wrote.** When a feature has cross-module wiring (orchestrator computes Z, passes to spawnee, spawnee writes artifact A that a downstream consumer reads), at least ONE test must (a) drive the orchestrator's full code path AND (b) assert on the *content* of artifact A, not on the internal state the orchestrator wrote. **Rule of thumb:** for every feature that produces an on-disk artifact (prompt files, sentinels, lockfiles, marker files, IPC messages, JSONL events), ask "what does the downstream consumer actually read?" and assert on that, not on what the test fixture wrote.
- **When a helper gets a second caller in a different context, re-derive its predicate from first principles for the new caller before reusing it.** Same shape as the rule above ("primitive-absence repurposed") in another dimension: that one is about absent values; this is about absent semantics. The full test suite often misses this because one existing test has codified the buggy behavior as the expected output — locked-in tests are not validation, they are version-controlled assumptions about a single caller's context. **Pattern fix:** when adding a second consumer, audit (a) the helper's predicate (does it still hold under the new context's invariants?) AND (b) every test asserting on its output (was the expected output correct only for the original caller?). **Rule of thumb:** the moment a helper docstring needs the phrase "for X callers" or "in single-tenant mode" to stay accurate, the predicate is overfit to one caller.
- **An LLM-worker self-evaluator only validates the code path its tests cover; when the brief commits to multiple callers, the worker must drive each path end-to-end or the untested ones ship broken.** Brief coverage exceeds test coverage and the LLM worker doesn't notice. **Pattern fix:** in goal briefs that touch multiple callers/modes, the "What done looks like" section must enumerate each caller path and the verification-suggestions section must require at least one test that drives each. The planner that decomposes the brief should produce a separate task asserting integration on each caller. **Rule of thumb:** if your brief says "applies to X / Y / Z" but your unit-test suite only patches X, Y and Z are unverified and probably broken.
- **When a new mode bypasses a primitive, audit every consumer of that primitive's "absent/false" state.** A new mode that opts out of an underlying mechanism silently breaks consumers that previously read the primitive's missing-or-false signal as a load-bearing semantic event. **Pattern fix:** when adding a mode that opts out of a primitive, grep for every site that reads the primitive's absent/false state and add an explicit branch (`if !primitiveAlive && !isInNewMode`) OR widen the primitive (carry an explicit `mode` enum through the consumer). NEVER reuse a code path designed for failure-of-the-primitive to mean "the primitive doesn't exist." **Rule of thumb:** a single boolean serving two domains needs to become two booleans, OR an enum, before the second consumer is added.
- **A green test suite does not prove the production entry point will import.** Test runners (vitest, jest, pytest) collect modules in filesystem/dependency order and may resolve circular imports differently from a cold start. **Pattern fix:** for any module that is a process entry point (anything launchd / systemd / a CLI / `node X` / `python -m X` invokes), add a regression test that imports it in a fresh subprocess: `execSync('node -e "require(\\"./dist/cli.js\\")"')`. Test runner's collection-order caching can't mask a cycle in a cold interpreter. **Architectural fix:** module-level state (singletons, registries) shared across cycle-prone boundaries belongs in its own no-deps leaf module, not in the entry-point module itself.
- **Don't write ad-hoc markdown parsers with single-pass regex.** Markdown looks string-shaped but is structural: `#` is a header at line start outside code fences, shell syntax inside ```` ``` ````, and a literal char in `` `inline` ``. A regex that "finds the next header" will pass the trivial test, ship, and then fail every time worker output uses a slightly different — but valid — markdown shape. *Cortextos has hit this trap; jarvis hit it three times in one session.* The right shape: either use a real CommonMark parser (`marked`, `markdown-it`, or `remark`), OR strip fenced regions and inline code first, THEN run a structural pass. If you must regex, the test list MUST include: nested sub-headers, fenced code with `#` lines, fenced code with `~~~` markers, indented code blocks, inline-code with backticks, blockquoted headers. Cover the *class*, not the surface case. **Meta-lesson:** per-symptom rules don't generalize. Write rules that name the *class of trap* you fell into.
- **Brief addendums are silently lost in autonomous-coder planner translation.** A planner reads the brief BODY into task descriptions but does NOT reliably propagate addendum blocks added after the initial plan. **Pattern fix:** if you add an addendum to a brief that already has a planned `state.json`, treat the plan as stale. Either (a) re-run the planner against the addended brief before tick, OR (b) hand-augment the affected task descriptions in `state.json` to name the addendum requirements verbatim. **Rule of thumb:** the prompt is built from `state.tasks[i].description`, not from `goals/<slug>.md`. If the addendum text doesn't appear in the description, the worker will never see it.
- **Configurability requirements need explicit consumer-wiring tests.** A brief that names a config file (TOML/JSON/env) MUST have a regression test asserting (a) the file is loaded by a real production code path, AND (b) a non-default value flows through to the documented behavior. **Pattern fix:** for every config requirement in a brief, add a test that (i) writes a config file with a non-default value, (ii) drives the consumer code path, (iii) asserts the consumer's behavior changed because of (i). A test that just opens the file and parses it doesn't satisfy this. **Rule of thumb:** if the brief says "configurable in `<path>`," grep your code for `<path>` or the config-loader API BEFORE shipping. If neither appears, the config file is decoration, not configuration.
- **Network-call timeouts must be sized to the server's observed p99, not to what "feels fast." Don't wrap a deterministically-too-short timeout in retry-once-after-Ns — that mis-labels the deterministic failure as "transient flake" and misdirects diagnosis.** **Pattern fix:** when a timeout is suspected, instrument with one direct uncatched call and *measure* the actual p99. Set the timeout to ~2x measured p99. If the call is genuinely flaky (variance > 3x), retry helps; if it's deterministically slow, retry is debt. **Test fix:** mock the underlying call to inject a 1.1x-timeout-duration delay and assert the request succeeds (i.e., the timeout has margin). **Rule of thumb:** an error message that implies data corruption ("DB inconsistent", "state divergence") is a strong signal to FIRST verify it isn't really "client gave up before server finished" — picking dramatic error labels for benign causes hides root causes for years.
- **Filesystem moves leave stale absolute paths in multiple places, and symptoms surface unevenly.** Moving a build directory (project relocation, USB volume reorg, monorepo→polyrepo splits) leaves the old path baked into shebangs, virtualenvs, lockfiles, package.json scripts, ESM `paths` configs, sourcemap URLs, etc. The trap: the move may keep some entry points working through internal resolution while breaking others. One symptom proves the move is broken; absence-of-symptoms does NOT prove it's healthy — call graphs differ. **Pattern fix:** after `mv`, run a one-liner sweep: `OLD=...; NEW=...; grep -rln "$OLD" "$NEW" | while read f; do sed -i '' "s|$OLD|$NEW|g" "$f"; done` and verify with empty grep. Then drive BOTH a CLI invocation AND an entrypoint invocation. **Rule of thumb:** prefer `npm install` (or equivalent rebuild) over `mv` when the relocation is permanent — those rebuild path-dependent files automatically. `mv` is a fast-path optimization that owes you a sweep.
- **Supervisor `KeepAlive: {SuccessfulExit: false}` doesn't restart on clean SIGTERM (exit 0); use plain `KeepAlive: true` for daemons.** The dict form treats exit 0 as "intentional shutdown — don't restart," which is correct for one-shot tasks but wrong for a long-lived process you want to keep alive. A `kill -TERM <pid>` trips this — daemon shuts down gracefully with exit 0, supervisor considers it done. Plain `KeepAlive: true` (launchd) or `Restart=always` (systemd) restarts on ANY exit.
- **A probe and the recovery action it implies must share one detector function — not two parallel implementations.** Reliability code typically grows two sibling check sites: (a) a probe that *reports* failure ("X is broken — restart needed") and (b) a health check called by the watchdog that decides *whether to restart*. When both are written by hand from the same intent, they drift. They look correct in isolation, the unit tests for each pass, but the union has an unhandled state where one says "broken" and the other says "fine" — and the watchdog never acts on the probe's alert. **Pattern fix:** define one `isHealthy(): {ok: boolean, why: string}` per subsystem. The probe formats its message from that one return value; the watchdog reads `ok` from the same return value. They cannot disagree because they're the same code. If you must keep them split (e.g., different processes), the test boundary is: for every probe, write a test that drives the recovery action against the same observable state and asserts the action also fires. **Rule of thumb:** if you grep your codebase for the probe's error string and find a sibling function that branches on the same set of process counts / file states / API responses but doesn't trigger recovery for at least one state the probe flags, you have this bug.
- **Tests that conflate two production roots into a single tmp dir hide tree-routing bugs forever; outbound-side-effect calls leak to real systems unless gated at the bottom of the stack.** Two-in-one trap. **(a)** When a class takes a "root" arg, ask whether downstream calls write to ≥2 distinct trees in production; if yes, take both args even if tests will pass `tmp` for both. Add at least one regression test that uses *different* dirs for each — the one that breaks the conflated-tmp test is the one that proves the split. **(b)** Outbound side effects (Telegram, Slack, ntfy, email, webhooks) need a `process.env.PYTEST_CURRENT_TEST || process.env.NODE_TEST` gate at the lowest call site that's not on a wire-format unit-test path. Individual callers forgetting to mock is a bug class — gate it once at the bottom of the alert stack with an explicit `ALLOW_OUTBOUND_IN_TEST=1` opt-back-in for the small set of tests that genuinely exercise the wire format. **Rule of thumb:** if a test creating a real-looking entity (agent, approval, alert) on `tmp` would page someone in production, you have either bug (a), bug (b), or both.

## Before writing code

1. Read `OVERVIEW.md` and `CLAUDE.md` at framework root for architecture context.
2. Search `git log --oneline` and `experiments/learnings.md` per agent for past mistakes around the area you are touching.
3. Check target file line count — if 300+, ask whether your addition belongs here or needs a new module.
4. If unsure where code goes → it's a separate file under `src/<area>/`.

## After writing code

- Re-read the diff before committing. The unread diff is where regressions hide.
- Run `npm run build` (TypeScript compile) and `npm test` (vitest). Both must pass green.
- Run `code-evaluator` subagent on every phase commit (see CLAUDE.md working loop). LGTM is the gate to next phase.

## Reliability lessons (compressed)

- **Long-poll connections need hard watchdogs.** A network library's `timeout: N` typically only covers connect + first-byte; once TCP is established, the timeout never fires on idle. Wrap loops in an outer deadline (e.g. `AbortController` with a setTimeout, or `signal.alarm()` in Python) at 2-3x expected duration.
- **Probe error rate counts unique failures, not raw events.** A single stuck probe firing every 5 min generates 12 errors/hr — masks systemic vs isolated. Count distinct failing specs.
- **Process counting matches argv[0], not substrings.** Parents preserve children's argv via wrappers (`tmux`, `bash -c`, `sudo`, `env`). Use a structured argv-aware matcher; reserve substring grep for "any process whose argv mentions X".
- **Heartbeat fields tick on every cycle, not only on activity.** A `last_poll`/`last_seen` that only updates when work happens is a *usage* signal. Either rename, or write on every iteration.
- **Watchdog recoveries preserve user state by default.** Default to `--continue`/`resume=true`; the restart-attempt cap prevents resume-loops if the session itself crashes. Marker-files are for *intentional* sticky-resume only (model switch, handoff).
- **Bracketed-paste TUIs need a separate Enter to submit.** `tmux send-keys "<text>" Enter` looks like submit but Claude Code v2.1+ buffers the burst as paste — Enter gets absorbed. Pattern: `send_keys(prompt)` → `sleep ~1s` → `send_keys("")` for standalone Enter. Symptom is silent.
- **Process detectors must be validated against a real running instance.** Argv formats drift across versions/wrappers (bun's `exec` strips path; Node's `--inspect` rewrites argv). Run the detector once against `ps -A` and verify count = reality. If identity is in cwd/env/fds, detect on that.
- **Liveness probes must be falsifiable end-to-end, not just structurally.** "Running with right name and count" doesn't prove it's *doing what it should*. Add a loopback probe that emits a sentinel from outside and verifies arrival. Outbound success without inbound verification is the most common silent-failure shape.
- **Before enabling a probe, run it against BOTH known-good AND known-bad state.** `enabled=true` in a test only proves config, not that the probe flips ok→error correctly. Two failure modes: probe structurally incapable of detecting its target, OR probe flags healthy state as broken. Ritual: run live, expect ok; introduce failure, expect error.
- **One logical change per PR, not just per commit.** Bundling an urgent hot-fix with a non-urgent refactor on one branch is a trap: the urgent fix gets re-derived elsewhere, the refactor rots, landing requires per-commit triage. For any open PR >7 days, check whether each commit's *content* was re-derived in main; cherry-pick survivors onto a fresh branch and supersede.

## cortextOS-specific micro-retros

(Phase 2 of feat/code-quality-rules — appended below in date order. See `experiments/learnings.md` per agent for full context.)

<!-- micro-retros to be appended in Phase 2 -->
